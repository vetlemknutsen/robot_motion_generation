[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "motion-pipeline"
version = "0.1.0"
dependencies = []

[tool.setuptools]
packages = ["motion_pipeline"]
